export function guard() {
  // This will be implemented to trigger the validation logic
  // For now, it's a placeholder as defineEnv already does the validation
  // during schema instantiation if used at the top level.
  // We might want to separate the "definition" from the "validation" logic later.
}
